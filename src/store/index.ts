import {auth, AuthState} from "./modules/auth.module";


type State = {
    auth: AuthState;
};

const store = null;
    // createStore({
    // auth
// });


export type {State};

export default store;